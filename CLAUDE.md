# Hexbiblio — Project Context

Academic thesis-sharing platform: submission, browsing, comments/ratings, an AI research-advisor chatbot, and personal collections. React + TypeScript + Vite frontend, Supabase backend (Postgres, Auth, Storage, Edge Functions).

## Current infrastructure (as of 2026-08-01)

- **Frontend hosting**: Vercel, auto-deploys from the `main` branch on GitHub (`Hexbiblio/hexbiblio`). Live at `https://www.hexbiblio.vercel.app`. No custom domain yet.
- **Backend**: Supabase project ref `lstlboldqghczcomgxvv` — this is the user's own project, fully independent. **Do not confuse with `xyfsajxypbjgtceoqbkh`**, an old Lovable Cloud-managed project that is no longer used and should be treated as decommissioned.
- **AI chatbot** (`supabase/functions/thesis-chat`): calls Google's Gemini OpenAI-compatible endpoint directly (`https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`) using a `GEMINI_API_KEY` secret set in Supabase. Model is `gemini-flash-latest` (auto-updating alias) — don't hardcode a dated version string here, `gemini-2.5-flash` got deprecated once already. This replaced a call to the Lovable AI Gateway (`LOVABLE_API_KEY`) that the function had used since it was first written — that secret was never set on the independent Supabase project after the Lovable Cloud migration, so **the chatbot had been silently broken in production** until this switch. **Verified working in prod as of 2026-07-30** (both `thesis-chat` and `submit-thesis`, below, share this same secret and key). Get a `GEMINI_API_KEY` from https://aistudio.google.com/apikey and set it as a Supabase Edge Function secret if it's ever missing/rotated again.
- **Quest system** (`src/components/ThesisQuests.tsx`): a 6-step roadmap (discipline → theme → question → thesis → method → sources) tracked per-user in `localStorage` (not synced server-side beyond the profile fields each quest writes to, see `QUEST_PROFILE_FIELD`). `detectCompletedQuests` only ever checks the *next* open quest against the user's latest message — strictly sequential, a later-step cue can't skip ahead of an earlier open one. `thesis-chat` also receives `currentQuest`/`completedQuests` in the request body and is prompted to redirect the student back to the current step if their message jumps ahead, rather than just following the tangent.
- **Chat self-introduction** (added 2026-07-31): `thesis-chat`'s system prompt now tells Gemini to open its very first reply in a conversation with a one-sentence self-introduction ("I'm Hexbiblio, your research mentor...") before greeting/asking the opening question — it infers "first reply" from there being no earlier assistant turn in the `messages` history it receives, there's no explicit flag for it. Never repeats later in the same conversation, per the same instruction.
- **Onboarding card — mandatory** (added 2026-07-31, made mandatory same day): a separate concept from the Quest system above — Quests track *thesis-topic* progress (discipline/theme/question/etc.), this is about the *student's own* profile context (academic level, field of study, personal interests) used to personalize guidance. [`OnboardingCard.tsx`](src/components/OnboardingCard.tsx) renders in [`ChatInterface.tsx`](src/components/ChatInterface.tsx)'s empty state whenever a logged-in student's `profiles.first_name`, `academic_level` or `field_of_study` is still empty. It's a hard gate, not a nudge: there's no skip, and when it's showing, nothing else in the empty state renders — no greeting, no input box, no suggested questions, no "how it works" — the student cannot chat until it's done. `first_name`, `academic_level` and `field_of_study` are the required fields (Save stays disabled until all three are set); `last_name` and `research_interests` (up to 5 tags) are genuinely optional, labeled as such, and don't gate anything. **`first_name` is collected here specifically because nothing else asked for it before the student needed it** — `handle_new_user()` only ever populates `username`, and the `set_thesis_author_name` trigger derives a thesis's public author from first+last name, so a student who finished onboarding would previously hit a wall at their first submission and get bounced to `Profile.tsx`. The last name is asked for at submission time instead, inline in [`SubmitThesis.tsx`](src/pages/SubmitThesis.tsx) (the one moment it's actually required), and persisted to the profile before the insert so the trigger can read it and so it's never asked for twice — keeping onboarding itself short while preserving full-name attribution. This re-evaluates on every visit from `profiles` server state (not a `localStorage` flag) — a student who filled it in previously via `Profile.tsx` directly won't be gated again, since the same two columns are what both surfaces read/write; conversely if those columns are ever cleared, the gate returns. `ACADEMIC_LEVELS` lives in [`fields.ts`](src/i18n/fields.ts) as a bilingual list (alongside `FIELDS`/`DEGREE_TYPES`) shared with `Profile.tsx` — previously `Profile.tsx` had its own English-only array. Visually it reuses the chat's own speech-bubble treatment (`MascotAvatar` + tailed bubble, see below) instead of a generic bordered card — the intent is "the mentor is asking you these questions," not a form, even though the fields themselves are still structured selects/tags (not free text) to keep the data clean against the same canonical lists used elsewhere (filters, thesis submission).
- **Chat bubble redesign** (added 2026-07-31): [ChatInterface.tsx](src/components/ChatInterface.tsx)'s message list now renders both roles as proper comic-style speech bubbles (rounded rectangle with a small diamond "tail" pointing back at the avatar it came from — a CSS trick, a 3×3 `rotate-45` square glued to the bubble's edge), replacing the old layout where only the user's message had a background and the assistant's was bare text. The user's avatar is a plain silhouette (`UserRound` from lucide). The assistant's avatar is [`MascotAvatar.tsx`](src/components/MascotAvatar.tsx) — a single-purpose wrapper around `public/mascot-book.png` (the user's own in-progress mascot artwork, cropped tight to its content via a one-off Python/Pillow script since the original export had a lot of transparent padding), used at both call sites (the message list and the loading indicator) instead of duplicating an `<img>` tag. It takes an optional `className` (defaults to `h-10 w-9`) so resizing it later is a prop change, not a hunt through the file. This is deliberately a thin wrapper — see the mascot follow-up below for why: the whole point of isolating it here is that swapping in the eventual animated version only touches this one file.
- **Admin role + moderation dashboard** (added 2026-07-31): `public.user_roles` (migration `20260731100000_add_admin_role_and_moderation.sql`) is a separate table, deliberately **not** a `profiles.role`/`is_admin` column — a client-writable role column governed only by RLS would be self-escalatable, the same bug class already fixed once for `theses.author_name`. It has zero INSERT/UPDATE/DELETE policy for `authenticated` — granting/revoking admin is only ever a manual SQL statement in the Supabase dashboard, never a feature the app exposes. A `SECURITY DEFINER STABLE` `is_admin(_user_id uuid)` function backs new permissive RLS policies ("Admins can update/delete any thesis", "Admins can update any profile" — additive to, not replacing, the existing owner-only ones, since Postgres ORs multiple permissive policies together) and an admin-aware rewrite of `lock_immutable_thesis_fields()` that skips the revert-to-`OLD` logic for admins, letting them edit `title`/`abstract`/`file_url` — locked for everyone else. A second migration (`20260801000000_admin_can_edit_author_name.sql`) applies the same admin bypass to `set_thesis_author_name()` — normally this trigger unconditionally re-derives `author_name` from the submitter's `profiles.first_name`+`last_name` on every insert/update (see Author lock below), but now skips that for admins so they can set it directly. Doesn't weaken the protection for anyone else: the function still checks `is_admin(auth.uid())` first, and on `submit-thesis`'s own INSERT path `auth.uid()` has no JWT context (it runs on the service-role key), so `is_admin()` is false there regardless — submission behavior is unchanged. Frontend: [`AuthContext.tsx`](src/contexts/AuthContext.tsx) exposes `isAdmin`/`isAdminLoading` (queried from `user_roles`, `as any`-cast like other tables missing from the stale generated `types.ts`); [`AdminRoute.tsx`](src/components/AdminRoute.tsx) gates `/admin` → [`AdminDashboard.tsx`](src/pages/AdminDashboard.tsx) (Mémoires/Comptes tabs, `table.tsx`-based); [`ThesisDetail.tsx`](src/pages/ThesisDetail.tsx)'s existing owner-only edit feature becomes `isOwner || isAdmin`, plus an admin-only delete button and admin-only title/author name/abstract/PDF-URL fields. Admin accounts also skip: the mandatory onboarding gate (`ChatInterface.tsx`), `thesis-chat`'s daily message cap, and `submit-thesis`'s daily cap + post-rejection lockout — all via the same `user_roles` lookup, done server-side in the edge functions so it can't be spoofed by anything the client sends. **Deliberately NOT bypassed for admin**: `thesis-chat`'s 3-second `MIN_INTERVAL_MS` spacing check (protects the shared `GEMINI_API_KEY` itself from a repeat of the 2026-07-29 ban, not just the student experience) and `submit-thesis`'s Gemini content-consistency check (a quality gate, not a rate control). Account deletion is a separate edge function, [`admin-delete-user`](supabase/functions/admin-delete-user/index.ts), since it needs the Auth Admin API (service-role-only) — it re-derives admin status server-side (never trusts a client-supplied flag) and refuses to let an admin delete their own account via that path. `profiles.user_id`/`theses.user_id` already `ON DELETE CASCADE` to `auth.users(id)`, so one Admin API call cleans up everything (theses, comments, ratings, bookmarks, sources) with no manual cleanup step. Migration and the one-time admin-grant `INSERT` were run by the user via the SQL Editor (the `CREATE POLICY` statements needed `DROP POLICY IF EXISTS` guards added after an initial paste half-applied and a re-paste hit "already exists" — same class of `db push`-vs-SQL-Editor gotcha as elsewhere in this file). **Verified working in prod as of 2026-08-01**: the user's own account confirmed as admin, "Admin" nav link appears, `/admin` loads instead of redirecting.
- **Thesis submission integrity** (added 2026-07-30, three phases):
  - *Author lock*: `theses.author_name` is no longer free text. [SubmitThesis.tsx](src/pages/SubmitThesis.tsx) pre-fills it (disabled) from the submitter's `profiles.first_name` + `last_name`, and a Postgres trigger (`set_thesis_author_name_trigger`, migration `20260730120000_lock_thesis_author_and_antispam.sql`, re-pointed at first/last name in `20260730140000_use_first_last_name_for_thesis_author.sql`) re-derives it from the profile on every insert/update regardless of caller — can't be spoofed via a direct API call either. `profiles.username` plays no role in authorship anymore — kept only for personalization (the `thesis-chat` greeting, comment author display in `CommentSection.tsx`).
  - *Basic anti-spam*: min length + non-repetitive-abstract checks client-side ([thesisValidation.ts](src/lib/thesisValidation.ts)) and via DB `CHECK` constraints + a unique index on `(lower(title), lower(author_name))`.
  - *AI content verification*: [submit-thesis](supabase/functions/submit-thesis/index.ts) downloads the uploaded PDF, extracts text from the first ~15 pages (see gotchas below — `unpdf` has no page-range option and blows the CPU budget on real multi-page PDFs otherwise), and asks Gemini whether it matches the declared title/field/abstract before inserting into `theses`. The `theses` `INSERT` RLS policy for `authenticated` clients was dropped (migration `20260730130000_lock_theses_insert_via_function.sql`) — this function (service-role key) is the **only** path that can create a thesis row. PDF upload is accordingly mandatory. On a content mismatch, the client only ever sees a generic rejection message — Gemini's specific reasoning is logged server-side only, never returned, so a would-be spammer can't use the response to probe where the checker's line is — and the account is locked out of further submissions for 1 hour. **Both the accept and reject paths verified working in prod as of 2026-07-30.**
  - *Bibliography extraction (Phase 3)*: a `sources` table holds citations extracted from each thesis's PDF, via shared logic in [`_shared/extractSources.ts`](supabase/functions/_shared/extractSources.ts) (see gotchas below for why it locates the bibliography through the table of contents rather than a fixed page window). New submissions trigger extraction in the background from `submit-thesis` via `EdgeRuntime.waitUntil()` (never blocks the response); [extract-sources](supabase/functions/extract-sources/index.ts) backfills already-submitted theses in small batches, gated on the raw `SUPABASE_SERVICE_ROLE_KEY` as bearer token (internal/maintenance endpoint, not user-facing — never invoke it with anything other than that key). Browsable at `/sources` ([Sources.tsx](src/pages/Sources.tsx), filterable by the parent thesis's field/degree) and as a compact numbered list on each thesis's own detail page.
  - *Post-submission edits*: authors can change `field`/`degree_type`/`graduation_year`/`keywords` on their own thesis from [ThesisDetail.tsx](src/pages/ThesisDetail.tsx) (pencil icon next to the badges, owner-only). `title`/`abstract`/`file_url`/`user_id` stay immutable — a trigger (`lock_immutable_thesis_fields_trigger`, migration `20260730160000_allow_thesis_metadata_edits.sql`) reverts any attempted change to those columns regardless of caller, since the existing "Users can update own theses" RLS policy was never column-restricted and would otherwise let a direct API call rewrite the AI-verified title/abstract/PDF.
- **Les incontournables d'un domaine** (added 2026-08-01): ranks the works most students cite, per field — the first feature to exploit the `sources` table as a corpus rather than as a per-thesis list. Migration `20260801120000_field_essentials.sql` adds an IMMUTABLE `normalize_citation_title()` (lowercase, accent-fold, collapse whitespace, strip edge punctuation) plus a `get_field_essentials(_field, _limit, _min_theses)` RPC. Surfaced as the **default tab** on `/sources` via [`FieldEssentials.tsx`](src/components/FieldEssentials.tsx); the existing full list moved to a second tab, and its query is now gated on that tab being active so it doesn't fire unseen. Clicking a work jumps to the full list pre-searched for its title. Design notes worth keeping: (1) **grouping is by normalized title, deliberately not by author or year** — author formatting swings between "Bourdieu, P." and "Bourdieu, Pierre" and years differ across editions, so title is the only stable identifier the AI parse produces; (2) accent folding uses `translate()` rather than `unaccent()` so the migration needs no extension and stays genuinely IMMUTABLE (could back an index later); (3) there's a **minimum title length of 8 characters** — without it, bad parses like `"p. 42"` appear in many bibliographies and rank *first*, which is exactly what happened in testing; (4) ranking counts `DISTINCT thesis_id`, not rows, so a duplicate entry within one bibliography can't inflate a work's standing; (5) display title/authors/year are picked by frequency then **longest** — on a young corpus nearly every work is cited two or three times with a different spelling each time, so ties are the norm and the longer string is the more complete attribution. The whole thing was validated before deploy against real Postgres via PGlite (WASM) with a fixture reproducing all of the above — see git history if it needs revisiting.
- **Pagination on `/database` and `/sources`** (added 2026-08-01): both pages previously ran `select("*")` with no `limit`/`range`, fetching the entire matching result set on every keystroke — harmless today, would silently degrade as either table grows. [`PageControls.tsx`](src/components/PageControls.tsx) is a shared control (windowed page numbers with ellipsis, icon-only prev/next since page numbers don't need translated labels, a "13–24 sur 87"-style range line) driving `.range()` on the query with `{ count: "exact" }` for the total. `PAGE_SIZE = 24` on both — chosen because it divides evenly by the existing `sm:grid-cols-2 lg:grid-cols-3` grid, so a full page never ends on a half-empty row. On `/sources` this only applies to the "Toutes les sources" tab (see the essentials feature above, which has its own bounded `LIMIT` and no pagination). Search/filter changes go through wrapper handlers (`handleSearch`/`handleFieldFilter`/`handleDegreeFilter`) that reset to page 1 *and* update the underlying state in one call — resetting via a separate effect would cause a wasted extra fetch (once for the filter change, once more when the reset lands). A side benefit on `/database`: the ratings/accuracy_ratings follow-up queries now only cover the current page's thesis IDs instead of every matching row.
- **Share metadata / SEO cleanup** (added 2026-08-01): `index.html` had leftover Lovable residue (`lang="en"` on a French-first app, `<meta name="author" content="Lovable">`, `@Lovable` as `twitter:site`, unresolved `TODO` comments) and no `og:image` at all, so links shared on LinkedIn/WhatsApp/Twitter rendered as a bare rectangle. Fixed: `lang="fr"`, a real bilingual-brand-voice description (tutoiement, matches `landing.subtitle`), `og:url`/canonical pointing at the real prod URL, and a proper `og:image`/`twitter:image`. The image itself — `public/og-image.png` (1200×630, the standard OG size) — is generated by a one-off Python/Pillow script (not checked in; the PNG is), not hand-designed: colors are pulled directly from `src/index.css`'s dark-mode HSL tokens (not eyeballed/guessed), and the two typefaces are the exact ones the app already loads (Libre Baskerville for the "Hexbiblio" wordmark, Source Sans 3 for the tagline), downloaded from the same Google Fonts URL `index.css` imports — so the share card and the live app are visually the same brand, not a separate one-off design. Uses the mascot (`public/mascot-book.png`) as the hero element. **Verified Vercel actually serves this as a real image despite `vercel.json`'s catch-all `rewrites` rule** — tested against an already-deployed static file (`hexbiblio-logo.svg` returns its real content, not `index.html`), confirming Vercel's rewrite only applies when no matching static file exists in the build output, so `/og-image.png` needs no `vercel.json` change to be served correctly.
- **Field/discipline list** ([`fields.ts`](src/i18n/fields.ts)): expanded 2026-07-30 beyond the original STEM/humanities set to also cover Management, Marketing, Finance, Sports Science, Political Science, International Relations, Communication, Anthropology, and Arts — the original list was pushing a lot of real submissions into generic "Other", making field-based filtering on `/database` and `/sources` far less useful. `theses.field` has no DB-level constraint, so extending this list is a frontend-only change (no migration).
- **Brand voice** (applied 2026-07-31, from a voice/tone guide the user supplied as `hexbiblio_brand_voice_1.md`, one directory above the project root — not part of the repo): a mentor, not a tool. French uses tutoiement everywhere (no legal/CGU pages exist yet, so the doc's vouvoiement-on-legal-pages exception doesn't apply to anything today); copy leads with "your work"/"ton travail" before community framing; "Hexbiblio" is always one word with only the H capitalized (was inconsistently "HexBiblio" in several places — fixed in `index.html`, `Navbar.tsx`, `Index.tsx`). [`translations.ts`](src/i18n/translations.ts) is the single source of truth for nearly all UI copy and got a full rewrite in this voice, EN included (natural rewrites, not literal translations of the new FR). [SubmitThesis.tsx](src/pages/SubmitThesis.tsx)'s post-submission success message is degree-aware (`submittedWorkLabel`) — "ta thèse" for `degreeType === "PhD"`, "ton mémoire" otherwise, since French (unlike English) doesn't use one word for both. Both `thesis-chat` and `submit-thesis` edge functions got the same treatment for their user-facing error/rejection strings — a shared `msg(language, en, fr)` helper (duplicated in each file, no shared edge-function utils module exists yet) now actually uses the `language` field both functions already received in their request bodies but previously ignored, and generic errors no longer leak raw exception messages (`e.message`) to the client — those go to `console.error` only now, a real bug fix as well as a voice one. **Not yet redeployed to prod** — both functions need `npx supabase functions deploy thesis-chat --project-ref lstlboldqghczcomgxvv` and the same for `submit-thesis` before these changes take effect live. Out of scope for this pass: visual branding (mascot/palette/logo — pure design, doc doesn't touch it), and a "N students found this useful" pluralized community counter the doc describes wording for but that doesn't exist as a feature anywhere in the app yet.
- **Auth providers**: Email/password (native Supabase) and Google OAuth (native `supabase.auth.signInWithOAuth`, configured with the user's own Google Cloud OAuth client). Apple sign-in is **intentionally hidden** in `src/pages/Auth.tsx` (commented out) — the user doesn't have a paid Apple Developer account yet. Re-enabling it requires that account plus a Services ID / Team ID / Key ID setup in Supabase's Apple provider.
- **SPA routing on Vercel**: [vercel.json](vercel.json) has a catch-all rewrite (`/(.*)` → `/index.html`) — without it, refreshing or directly linking to any client-side route (e.g. `/sources`, `/database/:id`) hit Vercel's own 404 page instead of the app, since only `index.html` exists as a real file and React Router (`BrowserRouter`) handles the rest client-side.
- **Local dev**: project lives at `/Users/simonlegall/Desktop/All Inside/HexBiblio/Code/hexbiblio` on the user's Mac. `.env` is gitignored — recreate locally with the project's real `VITE_SUPABASE_*` values if missing (ask the user, don't guess).

## History worth knowing

- The project was originally built and hosted entirely on **Lovable** (Lovable Cloud managed backend + `*.lovable.app` hosting + a proprietary `@lovable.dev/cloud-auth-js` OAuth wrapper). All of that has been fully migrated away:
  - Hosting moved to Vercel.
  - Database/auth/storage migrated from the Lovable-managed Supabase project to the user's own project (`lstlboldqghczcomgxvv`), via a `pg_dump`/`pg_restore` export-and-restore of the `public` schema, `auth.users`/`auth.identities`, and storage bucket configs. Real user accounts (passwords included) survived the migration intact.
  - The Lovable proprietary Google/Apple OAuth wrapper was replaced with native `supabase.auth.signInWithOAuth`.
  - Lovable Cloud has since been (or is about to be) fully disconnected in Lovable's own UI (Cloud → Advanced settings → Remove Lovable Cloud).
- A security audit was done and fixed:
  - `thesis-chat` previously had **no real auth check** (always sent the shared anon key, never the caller's session token) and **no server-side rate limiting** — only a client-side, trivially-bypassable guest limit. Fixed: the function now verifies the caller's JWT via `supabase.auth.getUser()`, rate-limits by `user_id` (logged in) or `guest:<ip>` (anonymous) using a `chat_logs` table, and re-fetches the caller's profile server-side instead of trusting a client-supplied `profile` field.
  - The `theses` storage bucket had no file-size/MIME restrictions and its upload policy let any authenticated user upload into any other user's folder. Fixed: capped at 20MB/PDF-only, upload policy now checks `auth.uid() = (storage.foldername(name))[1]`.
  - `.env` was previously committed to the GitHub repo. It's now gitignored (values there are the public/anon Supabase key, not high-severity, but still cleaned up).
- On 2026-07-29, a burst of rapid automated test requests against live `thesis-chat` got the Google Cloud project behind `GEMINI_API_KEY` **permanently denied access** by Google (abuse detection, not quota/billing). Recovered by generating a new key under a different Google account. A project skill, `.claude/skills/thesis-chat-testing-safety/SKILL.md`, now enforces human-like spacing (~3-5s minimum) between any live test calls to *any* AI endpoint in this project — read it before testing `thesis-chat` or `submit-thesis` live.
- On 2026-07-30, the user pasted a live `service_role` key (full admin DB access) directly into chat while sharing a `curl` command for the `extract-sources` backfill endpoint. Flagged immediately and rotated from the dashboard. Same pattern as an earlier exposed Google OAuth client secret (below) — when asking the user to run an authenticated `curl`/CLI command, ask for the **output only**, never the full command if it embeds a credential.

## Conventions / gotchas for future work

- **Never interpolate a search term straight into a PostgREST `.or(...)` filter** — PostgREST reads the filter body as a comma-separated list of conditions, so any term containing a comma splits it into garbage. This shipped and broke author-style searches (`"Dupont, M."`) on `/database`, `/sources` and the admin dashboard — worst on `/sources`, where citations are made of commas. Use `buildIlikeOrFilter` in [`searchFilter.ts`](src/lib/searchFilter.ts), which quotes each value (and escapes `"`/`\`); it's covered by tests in `searchFilter.test.ts`.
- **`npm audit`'s headline count is misleading here** — it counts dev-only tooling alongside things actually served to visitors, and its `--omit=dev` split mis-classifies both ways (`tailwindcss` sits in `dependencies` but is build-only; `ws` is a real runtime dep of `supabase-js` but is only used in Node, since browsers use native WebSocket). Before reacting to a number, trace what actually reaches the browser bundle. As of 2026-08-01 the count is 4, all needing breaking changes: `esbuild` (dev server only) and one remaining `react-router` open-redirect advisory that requires the v7 major.

- Supabase's web SQL Editor does **not** support `COPY ... FROM stdin` blocks (psql-only) — any raw SQL given to the user to paste there must use plain `INSERT INTO ... VALUES (...)` statements.
- Don't manually cast JSON-shaped text (`{"key": "value"}`) to `::text[]` just because it starts with `{` — that broke `auth.users.raw_app_meta_data`/`raw_user_meta_data` (jsonb) during the migration. Let Postgres coerce plain string literals to the target column type instead.
- `storage.buckets`/`storage.objects` have a `protect_delete` trigger — direct `DELETE` on them is blocked by Supabase; use `ON CONFLICT DO NOTHING` on insert instead of delete-then-reinsert patterns.
- The user is comfortable following precise step-by-step instructions (GitHub web UI, Supabase dashboard, Google Cloud Console) but had no prior local git/terminal experience — this local clone is their first. Keep terminal commands explicit and check in after each one rather than batching many steps.
- **Supabase Storage keys must be S3-safe ASCII** — accented characters (very common in French filenames, e.g. "Mémoire.pdf") and other non-ASCII characters are rejected outright with `Invalid key`. Sanitize filenames (strip diacritics, e.g. via `.normalize("NFD")` + stripping combining marks, then swap any other unsafe character for `_`) before using them in a storage path — see `sanitizeFileName` in [SubmitThesis.tsx](src/pages/SubmitThesis.tsx).
- **`npx supabase db push --linked` fails** with a `permission denied to alter role` / login-role error — known unresolved CLI bug ([supabase/cli#5091](https://github.com/supabase/cli/issues/5091)). Workaround: `npx supabase db push --db-url "postgresql://postgres:PASSWORD@db.lstlboldqghczcomgxvv.supabase.co:5432/postgres"` (direct connection string from Dashboard → **Connect** button → **Direct** tab; DB password is separate from the anon/service_role keys — reset it from Project Settings → Database if never saved). This project has repeatedly hit `db push` recording a migration's version as applied (in `supabase_migrations.schema_migrations`) even though the run errored — the DDL itself had already succeeded by the time the bookkeeping insert failed (e.g. `duplicate key value violates unique constraint "schema_migrations_pkey"`). **Don't assume an errored push means the migration didn't apply** — verify the actual DB object first (e.g. `select tgname from pg_trigger where tgrelid = 'public.theses'::regclass;`), then mark it with `npx supabase migration repair --status applied <version> --db-url "..."` if it's genuinely already there, rather than troubleshooting the migration content. Same fix applies to a migration pasted directly into the SQL Editor, which the CLI never sees at all. Write new migrations with `IF EXISTS`/`IF NOT EXISTS` guards where possible to avoid needing this.
- **`unpdf`'s `extractText()` has no page-range option** — it walks every page of a PDF, which reliably exceeds a Supabase edge function's per-request CPU budget on a real 80-150 page thesis (`Error 546`, Supabase's `WORKER_LIMIT`). Extract text page-by-page instead (`pdf.getPage(n)` + `page.getTextContent()` on the `PDFDocumentProxy` from `getDocumentProxy()`), stopping at a fixed page count — see `extractLeadingText` in [submit-thesis/index.ts](supabase/functions/submit-thesis/index.ts).
- **Don't locate a section of a long PDF by guessing a fixed page window** (e.g. "the bibliography is somewhere in the last N pages") — real theses can carry huge annexes after it (one seen in this project: bibliography at page 82 of 227, followed by 140+ pages of interview transcripts), so any fixed window eventually misses it. Read the table of contents instead (always near the front, cheap and bounded) for the stated page number, validate against the actual heading nearby, and only fall back to a backward scan / blind guess if that fails — see `findBibliographySection` in [`_shared/extractSources.ts`](supabase/functions/_shared/extractSources.ts).

## Known follow-ups not yet done

- No custom domain yet (still on `hexbiblio.vercel.app`) — needed to fix Google's OAuth consent screen showing the raw Supabase URL instead of "Hexbiblio", and would look more professional generally.
- Apple sign-in is disabled pending a paid Apple Developer account.
- `npm audit` reports 19 vulnerabilities (1 low, 3 moderate, 14 high, 1 critical) — not yet investigated; likely dev-dependency noise but should be checked, not assumed safe.
- Old exposed Google OAuth client secret (shared in chat, then rotated) — confirm the old one was actually deleted from Google Cloud Console, not just superseded.
- The 3 seeded test users from the migration were the user's own test accounts — may already be deleted via Supabase Dashboard → Authentication → Users by the time you read this.
- **Book mentor mascot — animated version**: the user is designing an illustrated character (dark navy/purple cartoon book, cute eyes, a glowing hexagon accent nodding to "Hex" in Hexbiblio) — their end goal is an *animated*, Clippy-like interactive presence in the chat UI, not just a static image. That animated version is future work, not scoped yet. The static artwork is already in use via `MascotAvatar.tsx` (see above) — when the animated version is ready, that's the one file to change (swap its `<img>` for whatever the animation needs — Lottie, sprite, a stateful component reacting to loading/idle/etc.), not a hunt through `ChatInterface.tsx`.
